# --- H2 en memoria ---
spring.datasource.url=jdbc:h2:mem:telconova;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# --- JPA ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- Consola H2 ---
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# --- JWT (clave >= 32 chars) ---
app.jwt.secret=HayQueCambiarEstoPorUnSecretoCombinado-no-olvidarAunque-este-es-de-prueba?supongo-que-esta-bien
app.jwt.expiration-ms=86400000

# --- Swagger/OpenAPI ya estará expuesto en /swagger-ui.html ---
